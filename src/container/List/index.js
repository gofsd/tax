// @flow
import { connect } from "react-redux";
import List from "../../stories/screens/ListOfMakets"



const mapDispatchToProps = dispatch => ({

})

const mapStateToProps = state => ({
});
export default connect(mapStateToProps, mapDispatchToProps)(List);